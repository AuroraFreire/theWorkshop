[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "resolution_week2_auroraaa"
version = "0.0.1"
dependencies = []

[project.scripts]
todo = "resolution_week2_auroraaa.main:main"

[tool.hatch.build.targets.wheel]
packages = ["src/resolution_week2_auroraaa"]